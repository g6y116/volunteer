package g6y116.volunteer

import android.annotation.SuppressLint
import org.apache.commons.text.StringEscapeUtils
import java.text.SimpleDateFormat
import java.util.Date

object Util {
//    @SuppressLint("SimpleDateFormat")
//    fun getDate(date: Date = Date()): String = SimpleDateFormat("yyyyMMdd").format(date)
//
//    fun getDate(startDate: String, endDate: String): String {
//        return startDate.substring(4, 6) + "/" + startDate.substring(6, 8) + " ~ " + endDate.substring(4, 6) + "/" + endDate.substring(6, 8)
//    }
}

fun main() {
    println("")
}